import * as React from 'react';
import EditRepository from '../../components/Repositories/Repository/EditRepository/EditRepository';

export const RepoEdit: React.FC = (props) => {
    return (
      <EditRepository />
    );
};